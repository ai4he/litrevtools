# ============================================================================
# LitRevTools Environment Variables
# ============================================================================
#
# This file contains ONLY sensitive credentials and secrets.
# Non-sensitive configuration is in config.ts
#
# IMPORTANT: Never commit the actual .env file to version control!
#
# ============================================================================

# ----------------------------------------------------------------------------
# Gemini API Configuration (REQUIRED)
# ----------------------------------------------------------------------------
# Multiple API keys for automatic rotation (comma-separated)
# Example: GEMINI_API_KEYS=key1,key2,key3
# The system will automatically rotate between keys to avoid rate limits
#
# Get your API keys from: https://aistudio.google.com/app/apikey
GEMINI_API_KEYS=your_gemini_api_key1,your_gemini_api_key2,your_gemini_api_key3

# ----------------------------------------------------------------------------
# Google OAuth Configuration (Required for Web Platform)
# ----------------------------------------------------------------------------
# Get credentials from: https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID=your_google_client_id_here
GOOGLE_CLIENT_SECRET=your_google_client_secret_here

# ----------------------------------------------------------------------------
# JWT Secret (Required for Web Platform)
# ----------------------------------------------------------------------------
# IMPORTANT: Change this to a strong random string in production!
# Generate with: openssl rand -base64 32
JWT_SECRET=litrevtools-secret-key-change-in-production

# ----------------------------------------------------------------------------
# Semantic Scholar API Key (Optional - improves rate limits)
# ----------------------------------------------------------------------------
# Get your API key from: https://www.semanticscholar.org/product/api
# SEMANTIC_SCHOLAR_API_KEY=your_semantic_scholar_api_key

# ----------------------------------------------------------------------------
# Optional: Override Default Configuration
# ----------------------------------------------------------------------------
# These override values from config.ts if needed:
#
# GEMINI_MODEL=gemini-2.5-flash-lite
# PAPER_BATCH_SIZE=15
# DATABASE_PATH=./data/litrevtools.db
# OUTPUT_DIR=./data/outputs
# WEB_PORT=3000
# WEB_HOST=localhost
# WEB_BASE_URL=http://localhost:3000
# MAX_PARALLEL_REQUESTS=3
# SCREENSHOT_ENABLED=true
